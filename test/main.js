metainspector = require('../');
should = require('should');
require('mocha');

describe('metainspector', function(){
	describe('FUNCTIONNAME()', function(){
		it('should TASKNAME', function(done){
			should.exist(true);
			true.should().equal().true;
			done();
		};
	};
};